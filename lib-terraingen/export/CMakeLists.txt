if (MSVC)
    add_definitions(-D_CRT_SECURE_NO_WARNINGS)
endif()
add_library(exportlib imageExporter.cpp)
target_include_directories(exportlib PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ${PARENT_DIRECTORY}../)
include(CMakePrintHelpers)